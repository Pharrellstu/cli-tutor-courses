# Setup

This course needs **Node.js** (version 18 or newer).

Node.js runs your JavaScript code outside the browser — it's what executes both your greeter and the tests that check your work.

Once installed, the check below should print a version like `v22.16.0`.
